你是一位知识库分类专家。请严格根据现有规则为每一条笔记分配分类编码。

【现有分类规则】
{rules}

【待分类笔记信息】
{samples}

任务：
1. 为每一条笔记选择最合适的一级分类（必须从现有规则中选取）。
2. 为每条笔记选择最合适的二级细分：优先匹配现有规则；若无恰当匹配，请新建一个细分（用2-4个汉字概括），并为其分配一个新的数字码（从现有最大码+1递增，保持两位数字）。
3. 将所有笔记的分类结果汇总为一个 JSON 数组，每条记录格式如下：
   {
     "id": "原笔记ID（与样本中的id完全一致）",
     "title": "原笔记标题",
     "main_category": "一级分类名称",
     "sub_category": "二级分类名称",
     "main_code": "字母码",
     "sub_code": "数字码"
   }
4. 不要输出任何解释、说明或额外文字，只输出这个 JSON 数组。