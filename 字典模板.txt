你是一位知识库架构师。现有分类规则存在缺口，请根据下方样本进行补充。

【现有分类规则】
{rules}

【未打标笔记样本】
{samples}

任务：
1. 分析样本中出现的、现有规则未能覆盖的分类需求。
2. 在保留全部现有分类（不可修改其编码）的前提下，新增必要的一级分类和二级细分。
3. 新增一级分类代码：按字母顺序紧接现有最大码（如现有最大为 AP，则从 AQ 开始）。
4. 新增二级细分代码：从现有最大数字码+1 开始递增，始终保持两位数（11, 12, …）。
5. 只输出一个完整的 JSON 对象（与 category_rules.json 结构完全一致），不要包含任何解释或标记。

JSON 结构示例：
{
  "main_categories": [
    {"name": "教育学习", "code": "A"},
    ...
  ],
  "sub_categories": [
    {"name": "学习方法", "code": "01"},
    ...
  ],
  "auto_assign_dynamic": true,
  "default_main_code": "ZZ",
  "default_sub_code": "00"
}